/**
 * Vitest alias target for `ponder:schema` (default export + named re-exports).
 */
import * as schema from "../../../ponder.schema";

export * from "../../../ponder.schema";
export default schema;
